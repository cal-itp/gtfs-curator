[project]
name = "gtfs-curator-utils"
version = "0.1"
requires-python = ">=3.11.0, <3.12.0"

[tool.uv]
package = true

[dependency-groups]


[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["gtfs_curator_utils"]
